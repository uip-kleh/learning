let rank = `B`;
switch(rank) {
    case 'A':
        console.log(`rank is A`);
        break;
    case 'B':
        console.log(`rank is B`);
        break;
    case 'C':
        console.log(`rank is C`);
        break;
    default:
        console.log(`out of rank`)
        break;
}
